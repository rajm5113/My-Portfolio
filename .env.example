# Add these values in Vercel Project Settings → Environment Variables.
# Never prefix the API key with VITE_ and never commit a real key.
GROQ_API_KEY=

# Optional: overrides the chatbot's default model.
GROQ_MODEL=qwen/qwen3.6-27b
